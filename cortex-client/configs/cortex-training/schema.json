{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Snowflake-AI-Research/Arctic-Platform/cortex-client/configs/cortex-training/schema.json",
  "title": "Cortex Training model catalog",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "catalog",
    "lastReviewed",
    "models"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "schemaVersion": {
      "const": 1
    },
    "catalog": {
      "const": "cortex-training"
    },
    "lastReviewed": {
      "type": "string",
      "format": "date"
    },
    "models": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/model"
      }
    }
  },
  "$defs": {
    "capability": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "supported"
      ],
      "properties": {
        "supported": {
          "type": "boolean"
        },
        "maxContextTokens": {
          "type": "integer",
          "minimum": 1
        },
        "recommendedProfileId": {
          "type": "string",
          "minLength": 1
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "supported": {
                "const": true
              }
            }
          },
          "then": {
            "required": [
              "maxContextTokens",
              "recommendedProfileId"
            ]
          },
          "else": {
            "not": {
              "anyOf": [
                {
                  "required": [
                    "maxContextTokens"
                  ]
                },
                {
                  "required": [
                    "recommendedProfileId"
                  ]
                }
              ]
            }
          }
        }
      ]
    },
    "model": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "modelId",
        "capabilities"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "modelId": {
          "type": "string",
          "minLength": 1
        },
        "capabilities": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "inference",
            "sftLora",
            "sftFull",
            "rlLora",
            "rlFull"
          ],
          "properties": {
            "inference": {
              "$ref": "#/$defs/capability"
            },
            "sftLora": {
              "$ref": "#/$defs/capability"
            },
            "sftFull": {
              "$ref": "#/$defs/capability"
            },
            "rlLora": {
              "$ref": "#/$defs/capability"
            },
            "rlFull": {
              "$ref": "#/$defs/capability"
            }
          }
        }
      }
    }
  }
}
