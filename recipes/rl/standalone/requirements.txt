# Everything this recipe needs on top of the client itself.
#
#   uv pip install -e '.[cortex]'
#   uv pip install -r recipes/rl/standalone/requirements.txt

# Config entrypoints (`config=... lora_rank=32` on the command line).
chz

# Dataset loading; also pulled in by tinker-cookbook.
datasets

# Renderers (chat templates + supervised weights), ml_log metric logging, and the
# MATH environment / grading used by rl_loop.py. The [math-rl] extra carries the
# grading dependencies.
tinker-cookbook[math-rl] @ git+https://github.com/thinking-machines-lab/tinker-cookbook.git@nightly

# Optional: mirror metrics to Weights & Biases (set wandb_project=... to enable).
wandb
