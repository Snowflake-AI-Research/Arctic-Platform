# Long-context QA recipe — pinned Python dependencies.
#
# Assumes CUDA 12.9. `torch` and `flash-attn` are installed separately in the
# README (torch needs the PyTorch CUDA index; flash-attn needs --no-build-isolation).
# If you use a different CUDA version, change the torch index URL and the
# `cuda-bindings` pin accordingly.
#
# A few transitive deps (flashinfer-python, numpy, transformers) are version-pinned
# in overrides.txt (uv pip install --override), because vLLM 0.18.0's metadata pins
# them higher than this recipe is validated against. Keep those pins in overrides.txt
# only -- listing them here too would be redundant.

arctic-inference[server]
# Arctic
arctic-platform[rl]
cuda-bindings==12.9.4
datasets

# Serving / kernels (arctic-inference patches vLLM 0.18.0, so pin that exact version).
vllm==0.18.0
