# uv override-dependencies for the simple GSM8K recipe.
#
# vLLM 0.18.0's metadata pins newer/older transitive deps than this recipe is
# validated against. The recipe is validated with the versions below, so we
# force them with `uv pip install -r requirements.txt --override overrides.txt`,
# reproducing the original sequential "last install wins" behavior in a single
# resolve.
#
# transformers: vLLM 0.18.0 requires `transformers<5`; SkyRL pins
#   `transformers>=5.6.1`. Neither is satisfiable together. 4.57.6 is the
#   exact version the Arctic RL × SkyRL integration is validated against
#   (also matches the verl recipes for parity).
#
# datasets: pinned to keep the resolve deterministic across SkyRL bumps that
#   would otherwise float this to a 5.x release with incompatible behavior.
transformers==4.57.6
datasets==4.8.4
