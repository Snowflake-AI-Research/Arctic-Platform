# Txt2SQL (BIRD) recipe — pinned Python dependencies.
#
# Single-node 8-GPU GRPO on Qwen3-8B / BIRD-SQL. Same Arctic RL stack as the
# multi-node `arctic_platform/recipes/rl/skyrl/*` runs; just 1 node.
#
# Assumes CUDA 12.8 + Hopper (H100/H200) for FlashAttention 3. On A100/L40S
# set ATTN_IMPL=flash_attention_2 in the launcher and drop the FA3 install
# line below.
#
# transformers / datasets pins live in overrides.txt (vLLM 0.18.0 wants
# transformers<5, SkyRL pins transformers>=5.6.1; the override gives uv a
# single deterministic resolve).

arctic-inference[vllm]
# Arctic
arctic-platform[rl]
liger-kernel

# Ray must match across the cluster (relevant for the >1-node variants of
# this recipe). SkyRL pins this exact version.
ray==2.51.1

# SkyRL pinned at PR #1837 merge (the dispatch hook the arctic_rl integration
# requires). This gives you the pip-installed `skyrl` package; the launcher
# also needs an on-disk clone of the same commit at $SKYRL_HOME because the
# Arctic RL × SkyRL integration code (config/trainer/generator/BirdEnv) lives
# under `integrations/arctic_rl/` and is NOT part of the pip package.
skyrl @ git+https://github.com/NovaSky-AI/SkyRL@76f5f467c6804e8acc6273cc677098b7679b0315
skyrl-gym @ git+https://github.com/NovaSky-AI/SkyRL@76f5f467c6804e8acc6273cc677098b7679b0315#subdirectory=skyrl-gym

# Serving / kernels (arctic-inference patches vLLM 0.18.0).
vllm==0.18.0
