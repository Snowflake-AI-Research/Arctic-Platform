[mypy]
plugins = pydantic.mypy

# TODO(qwen35): vendored/copied PrimeRL code; skip type-checking (and don't follow
# imports into it) while the qwen3_5_moe loader is being integrated. RE-ENABLE by
# removing this section once the qwen35 implementation is refactored.
[mypy-arctic_platform.model.implementations.qwen35.*]
follow_imports = skip
ignore_errors = True
